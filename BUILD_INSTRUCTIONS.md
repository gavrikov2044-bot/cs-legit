# 🔧 Инструкция по сборке Externa CS2

## 📋 Требования

### Для сборки драйвера:
1. **Visual Studio 2022** с компонентом "Desktop development with C++"
   - Скачать: https://visualstudio.microsoft.com/
   
2. **Windows Driver Kit (WDK)**
   - Скачать: https://learn.microsoft.com/en-us/windows-hardware/drivers/download-the-wdk
   - Установить соответствующую версию для вашей Visual Studio

### Для сборки чита:
1. **Rust** (stable)
   - Установить: https://rustup.rs/

### Для загрузки драйвера:
1. **iqvw64e.sys** (Intel драйвер)
   - Скачать: https://github.com/TheCruZ/kdmapper/releases

---

## 🚀 Быстрая сборка (автоматически)

```batch
:: Запустите BUILD.bat
BUILD.bat
```

Скрипт автоматически:
1. Найдёт Visual Studio и WDK
2. Соберёт драйвер
3. Соберёт чит с встроенными драйверами (если есть)
4. Положит результат в папку `release/`

---

## 📝 Ручная сборка

### Шаг 1: Сборка драйвера

```batch
:: Открыть в Visual Studio
laith-km-driver\Driver.sln

:: Выбрать: Release | x64
:: Build → Build Solution (Ctrl+Shift+B)

:: Результат: laith-km-driver\km\x64\Release\laithdriver.sys
```

### Шаг 2: Подготовка драйверов

```batch
:: Создать папку assets
mkdir cheats\externa-rust-v2\assets

:: Скопировать наш драйвер
copy laith-km-driver\km\x64\Release\laithdriver.sys cheats\externa-rust-v2\assets\

:: Скачать и положить Intel драйвер
:: (скачать с https://github.com/TheCruZ/kdmapper/releases)
copy путь\к\iqvw64e.sys cheats\externa-rust-v2\assets\
```

### Шаг 3: Сборка чита

```batch
cd cheats\externa-rust-v2

:: Вариант A: С встроенными драйверами (1 файл со всем)
cargo build --release --features embed_drivers

:: Вариант B: Без драйверов (syscall режим)
cargo build --release

:: Результат: target\release\externa-rust-v2.exe
```

---

## 📦 Структура после сборки

### Полная версия (с драйверами):
```
release/
├── Externa-CS2-FULL.exe   ← Всё внутри!
└── driver/
    ├── laithdriver.sys    ← Для ручной загрузки
    └── iqvw64e.sys
```

### Стандартная версия:
```
release/
└── Externa-CS2.exe        ← Работает через syscall
```

---

## 🎮 Использование

### Полная версия (Externa-CS2-FULL.exe):
```
1. Запустить CS2
2. Запустить Externa-CS2-FULL.exe от имени Администратора
3. Драйвер загрузится автоматически!
4. INSERT - вкл/выкл ESP
5. END - выход
```

### Стандартная версия + внешние драйверы:
```
1. Скачать kdmapper.exe
2. kdmapper.exe laithdriver.sys
3. Запустить CS2
4. Запустить Externa-CS2.exe от имени Администратора
```

### Стандартная версия (syscall):
```
1. Запустить CS2
2. Запустить Externa-CS2.exe от имени Администратора
3. Работает без драйвера!
```

---

## ⚠️ Решение проблем

### "WDK not found"
- Установите WDK с сайта Microsoft
- Версия WDK должна соответствовать Visual Studio

### "Driver build failed"
- Проверьте что WDK правильно установлен
- Откройте Driver.sln вручную и посмотрите ошибки

### "iqvw64e.sys not found"
- Скачайте с https://github.com/TheCruZ/kdmapper/releases
- Положите в cheats/externa-rust-v2/assets/

### Чит крашится
- Проверьте externa.log
- Запустите от Администратора
- Проверьте что CS2 запущен

---

## 🔒 Примечания по безопасности

- Драйвер работает в ring-0 (ядро Windows)
- Для загрузки неподписанного драйвера нужен:
  - Test Signing mode: `bcdedit /set testsigning on`
  - Или kdmapper (использует уязвимость Intel)
- Secure Boot должен быть выключен в BIOS

